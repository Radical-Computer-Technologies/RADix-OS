name=rad-tzdata
version=0.1.3-experimental
kind=data
data=/usr/share/zoneinfo
config=/etc/timezone,/etc/localtime
requires=rad-core
note=RADPx-OS timezone data package; default image includes UTC/local selectors and package-owned zoneinfo path.
